--- a/command_line_interface.py
+++ b/command_line_interface.py
@@ -293,7 +293,7 @@
             asr_new[:, :, 1:] = asr[:, :, 0:-1]
             asr = asr_new
         out = model.decoder(asr, f0_pred, n_pred, ref.squeeze().unsqueeze(0))
-    return out.squeeze().cpu().numpy()[..., :-100], s_pred  # remove pulse artifact at end
+    return out.squeeze().float().cpu().numpy()[..., :-100], s_pred  # remove pulse artifact at end
 
 
 # get reference style
--- a/utils.py
+++ b/utils.py
@@ -18,7 +18,7 @@
   """
   device = neg_cent.device
   dtype = neg_cent.dtype
-  neg_cent =  np.ascontiguousarray(neg_cent.data.cpu().numpy().astype(np.float32))
+  neg_cent =  np.ascontiguousarray(neg_cent.data.float().cpu().numpy())
   path =  np.ascontiguousarray(np.zeros(neg_cent.shape, dtype=np.int32))
 
   t_t_max = np.ascontiguousarray(mask.sum(1)[:, 0].data.cpu().numpy().astype(np.int32))
